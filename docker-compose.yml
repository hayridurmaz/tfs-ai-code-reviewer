version: '3.8'

services:
  reviewer:
    build: .
    container_name: ai-pr-reviewer
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data
    # Command is handled by Dockerfile (./reviewer)
